# Environment Variables for Render.com Deployment

## Backend Service Environment Variables:

### Database
# Staging and production should use different database names on the same
# cluster (e.g. .../peer-evaluation-staging vs .../peer-evaluation) so
# staging traffic and test data never touch real data -- see DEPLOYMENT_GUIDE.md.
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/peer-eval

### Email Configuration (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

### Frontend URL (will be provided after frontend deployment)
FRONTEND_URL=https://your-frontend-url.onrender.com

### Optional
NODE_ENV=production
PORT=5000