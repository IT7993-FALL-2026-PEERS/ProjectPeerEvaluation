{
  "name": "peer-evaluation-system",
  "version": "1.0.0",
  "private": true,
  "description": "Thin orchestration only -- no app code or app dependencies live here. The backend (src/backend) and frontend (src/frontend) are each self-contained npm packages with their own package.json, node_modules, Dockerfile and tests; this file just provides convenient top-level commands that shell out to both.",
  "scripts": {
    "install:all": "npm install --prefix src/backend && npm install --prefix src/frontend",
    "setup": "npm run install:all",
    "postinstall": "npm run install:all",
    "start:backend": "npm start --prefix src/backend",
    "start:frontend": "cross-env HOST=0.0.0.0 npm start --prefix src/frontend",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "test:backend:unit": "npm run test:unit --prefix src/backend",
    "test:backend:integration": "npm run test:integration --prefix src/backend",
    "test:frontend:unit": "npm run test:unit --prefix src/frontend",
    "test:frontend:integration": "npm run test:integration --prefix src/frontend"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "cross-env": "^7.0.3"
  }
}
