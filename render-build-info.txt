# Build settings for Render.com

# Backend service (Docker runtime, see render.yaml)
# Dockerfile path: src/backend/Dockerfile
# Docker context: src/backend
# The image runs `node index.js` from within src/backend (see Dockerfile CMD),
# equivalent to the previous native build/start commands:
#   Build:  cd src/backend && npm install
#   Start:  cd src/backend && node index.js

# Frontend build command (self-contained under src/frontend, like the
# backend; Render always runs buildCommand from the repo root regardless.
# The version.txt step is how the CI/CD pipelines confirm a deploy picked up
# the right commit -- see render.yaml)
cd src/frontend && echo "$RENDER_GIT_COMMIT" > public/version.txt && npm install && npm run build

# Frontend publish directory
src/frontend/build